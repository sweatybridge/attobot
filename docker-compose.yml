services:
  db:
    build:
      context: .
      args:
        PG_DURABLE_REPO: sweatybridge/pg_durable
        PG_DURABLE_VERSION: "0.2.2"
        PG_DURABLE_SHA256: 78ff0dc6804608ba7b7fa88849188f2f5c2ddc266d8aaf690c51e15bf26dbe91
        PG_MAJOR: "18"
    environment:
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: postgres
      ATTOBOT_API_KEY: ${ATTOBOT_API_KEY:-}
      ATTOBOT_MODEL: ${ATTOBOT_MODEL:-deepseek-v4-pro}
      ATTOBOT_API_BASE: ${ATTOBOT_API_BASE:-https://api.deepseek.com/v1}
    ports:
      - "5432:5432"
    volumes:
      - attobot_pgdata:/var/lib/postgresql/data

volumes:
  attobot_pgdata:
